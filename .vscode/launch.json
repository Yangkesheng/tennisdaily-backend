{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug TennisDaily API",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/api",
      "cwd": "${workspaceFolder}",
      "env": {
        "CONFIG_PATH": "${workspaceFolder}/config.yaml"
      }
    }
  ]
}
